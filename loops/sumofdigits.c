#include<stdio.h>
int main(int argc, char const *argv[])
{
    int n;
    printf("Enter a number :");
    scanf("%d", &n);
    int sum=0;
    printf("the remainder is :%d", n = n%10);
    // while (/* condition */)
    // {
    //     /* code */
    // }
    
        return 0;
}
